providers:
    - CleaniqueCoders\ConfigWebhook\ConfigWebhookServiceProvider

migrations:
    - workbench/database/migrations

workbench:
    install: true
    discovers:
        web: true
        commands: true
    build:
        - create-sqlite-db
        - migrate
